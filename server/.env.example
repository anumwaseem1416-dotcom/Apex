DATABASE_URL="postgresql://USER:PASSWORD@HOST:5432/DBNAME?sslmode=require&schema=public"
# If your local `psql`/libpq rejects `channel_binding=require`, omit it.
# Neon typically requires SSL: keep `sslmode=require`.
JWT_SECRET="change-me"
